;;; local-post.el.example --- Machine-local settings loaded after shared config -*- lexical-binding: t; -*-

;;; Commentary:
;; Copy this file to ~/.emacs.d/local-post.el for overrides that depend on the
;; shared modules already being loaded.

;;; Code:

;; Example: change a language server locally.
;; (with-eval-after-load 'eglot
;;   (add-to-list 'eglot-server-programs '(python-mode . ("pyright-langserver" "--stdio"))))

;; Example: add local bibliography or PDF paths.
;; (with-eval-after-load 'citar
;;   (setq citar-bibliography '("~/org/references.bib" "~/work/references.bib")))

;;; local-post.el.example ends here

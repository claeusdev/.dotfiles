# Ghostty Terminal Configuration

# Font configuration
# Same faces as Emacs: Iosevka Comfy for text, Symbols Nerd Font for icons.
font-family = "Iosevka Comfy"
font-family = "Symbols Nerd Font Mono"
font-size = 17
font-thicken = true

# Window configuration
# Roomier padding than the default: the desktop runs rounded window corners,
# and tight padding puts the first column right up against the curve.
window-padding-x = 14
window-padding-y = 12
window-padding-balance = true
window-decoration = true
window-theme = auto

# Theme and colors
# Both halves at once: Ghostty follows the desktop colour-scheme, which
# theme-mode(1) sets, so the terminal turns over with everything else and needs
# no push of its own.
theme = light:Modus Operandi,dark:Modus Vivendi Tinted
# Translucent so the desktop blur reads through the terminal.  The blur
# itself is NOT ghostty's `background-blur` -- that is a macOS and
# KDE/Wayland path and does nothing on GNOME/X11.  Blur My Shell provides it
# instead, whitelisted to com.mitchellh.ghostty by `desktop-theme`.
background-opacity = 0.92
unfocused-split-opacity = 0.95

# Shell
shell-integration = fish
shell-integration-features = cursor,sudo,title

# Keybindings
keybind = ctrl+shift+c=copy_to_clipboard
keybind = ctrl+shift+v=paste_from_clipboard
keybind = ctrl+shift+t=new_tab
keybind = ctrl+shift+w=close_surface
keybind = ctrl+tab=next_tab
keybind = ctrl+shift+tab=previous_tab

# Performance
copy-on-select = true

# Cursor
cursor-style = block
cursor-style-blink = true
